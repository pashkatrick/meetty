endpoint returning a user obkect by username or id
---
tags:
  - Users
parameters:
  - name: username
    in: path
    type: string
    required: true
  - name: full
    type: boolean
    in: path
    default: true
responses:
  200:
    description: A list of colors (may be filtered by palette)